[project]
name = "cli-mcp-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AJoelCastro", email = "misthorn33@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.1.1",
]

[project.scripts]
cli-mcp-example = "cli_mcp_example:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
